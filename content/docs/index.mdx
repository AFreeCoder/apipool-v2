---
title: APIPool Quickstart
description: Create a key, add credit, and make your first API call in minutes.
---

## Base URL

```text
https://app.apipool.dev
```

One provider-neutral endpoint and one key for every model. Add the protocol path
that matches your SDK or provider-compatible format:

- **OpenAI-compatible protocol (default)** — every model, regardless of vendor,
  can be called through `/v1/chat/completions`. Most SDKs and tools work by
  appending the OpenAI-compatible `/v1` prefix to the APIPool endpoint.
- **Vendor-native protocols** — the same endpoint and key also serve native
  formats, such as Anthropic's Messages API at `/v1/messages`. Use these when
  your tooling expects a vendor's own protocol.

## Authentication

Send your key in the `Authorization` header:

```text
Authorization: Bearer $APIPOOL_API_KEY
```

Create keys in the [console](/dashboard/api-keys). The full key is shown only
once after creation — the list shows a masked value afterward.

## Get started

1. [Sign up](/sign-up) and open the console.
2. Add credit on the [balance page](/dashboard/billing). Usage is billed per
   token; your balance never expires.
3. [Create an API key](/dashboard/api-keys) and copy it once.
4. Call any model from the [model list](/models) using the examples below.

## curl

```bash
APIPOOL_ENDPOINT="https://app.apipool.dev"

curl "$APIPOOL_ENDPOINT/v1/chat/completions" \
  -H "Authorization: Bearer $APIPOOL_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"gpt-5.6-luna","messages":[{"role":"user","content":"Hello from APIPool"}]}'
```

## OpenAI SDK (JavaScript)

```ts
import OpenAI from 'openai';

const APIPOOL_ENDPOINT = 'https://app.apipool.dev';

const client = new OpenAI({
  baseURL: `${APIPOOL_ENDPOINT}/v1`,
  apiKey: process.env.APIPOOL_API_KEY,
});

const completion = await client.chat.completions.create({
  model: 'gpt-5.6-luna',
  messages: [{ role: 'user', content: 'Hello from APIPool' }],
});

console.log(completion.choices[0].message.content);
```

## OpenAI SDK (Python)

```python
import os
from openai import OpenAI

APIPOOL_ENDPOINT = "https://app.apipool.dev"

client = OpenAI(
    base_url=f"{APIPOOL_ENDPOINT}/v1",
    api_key=os.environ["APIPOOL_API_KEY"],
)

completion = client.chat.completions.create(
    model="gpt-5.6-luna",
    messages=[{"role": "user", "content": "Hello from APIPool"}],
)

print(completion.choices[0].message.content)
```

## Plain HTTP (fetch)

```ts
const APIPOOL_ENDPOINT = 'https://app.apipool.dev';

const response = await fetch(`${APIPOOL_ENDPOINT}/v1/chat/completions`, {
  method: 'POST',
  headers: {
    Authorization: `Bearer ${process.env.APIPOOL_API_KEY}`,
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    model: 'gpt-5.6-luna',
    messages: [{ role: 'user', content: 'Hello from APIPool' }],
  }),
});

console.log(await response.json());
```

## Native protocols (Anthropic Messages)

Claude models can also be called with Anthropic's native Messages API — same
base URL, same key:

```bash
APIPOOL_ENDPOINT="https://app.apipool.dev"

curl "$APIPOOL_ENDPOINT/v1/messages" \
  -H "x-api-key: $APIPOOL_API_KEY" \
  -H "anthropic-version: 2023-06-01" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-sonnet-4",
    "max_tokens": 1024,
    "messages": [{ "role": "user", "content": "Hello!" }]
  }'
```

The official Anthropic SDKs work by setting `baseURL` to
`https://app.apipool.dev` and `apiKey` to your APIPool key.

## Embeddings and Images

Use `/v1/embeddings` for embedding models. Embeddings are billed for input
tokens only. Replace the model with an available Embedding model from the
[model list](/models):

```bash
curl "$APIPOOL_ENDPOINT/v1/embeddings" \
  -H "Authorization: Bearer $APIPOOL_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"YOUR_EMBEDDING_MODEL","input":"Hello from APIPool"}'
```

Use `/v1/images/generations` for image generation and multipart
`/v1/images/edits` for editing. Image models are billed **per returned image**
using the quality/size SKU prices on the model page. Images do not support
`stream:true`.

```bash
curl "$APIPOOL_ENDPOINT/v1/images/generations" \
  -H "Authorization: Bearer $APIPOOL_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"gpt-image-2","prompt":"A product photo on a white background","quality":"low","size":"1024x1024"}'
```

## Streaming

Chat Completions, Responses, and Messages accept `"stream": true` for SSE.
Images endpoints do not support streaming responses.

## Error reference

| Status                           | Meaning                                    | What to do                                     |
| -------------------------------- | ------------------------------------------ | ---------------------------------------------- |
| `401`                            | Key missing, invalid, disabled, or deleted | Check the header and key status in the console |
| `403`                            | Key has no access to the requested model   | Review the key's model restrictions            |
| `429` insufficient_quota         | Portal balance is exhausted                | Add credit in the console                      |
| `429` concurrency_limit_exceeded | Concurrency risk limit reached             | Retry using the `Retry-After` value            |
| `404` model_not_found            | Model ID not available                     | Pick a model from the [model list](/models)    |

## Billing

Top up in dollars on the [balance page](/dashboard/billing). Text and Embedding
usage is charged using the listed meter/token prices; per-call models such as
Images use the selected SKU price and actual returned unit count. Requests,
tokens, per-model spend, and request charges appear on the usage and billing
pages.
