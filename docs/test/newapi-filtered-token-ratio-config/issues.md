# 遗留验收项

- [ ] 部署后在 NewAPI 管理控制台新增禁用、无模型的“官方过滤倍率”渠道，Base URL 为 `http://newapi-metadata-filter:8080`；在“计费与支付 → 模型定价 → 上游价格同步”选择该渠道，以默认 `/api/pricing` 获取差异。确认 `gpt-5.4-mini` 显示 `0.375 / 6 / 0.1` 后批量应用；确认本次同步未产生任何 `model_price` 更新，按次计费模型仍保持本地手工配置。
