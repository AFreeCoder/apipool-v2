# 管理后台路由与钱包入口收敛测试报告

日期：2026-07-17

## 结论

通过。目录驱动路由、价格版本、本地钱包调额、支付入账、管理入口与旧设计退出主链路均有自动化验证，生产构建成功。

## 验证结果

- `pnpm exec tsc --noEmit --pretty false`：通过。
- 重点回归：89/89 通过，覆盖目录管理、可调用查询、路由快照、后台概览、调额权限、钱包充值和网关管理 API。
- 支付与路由集成回归：50/50 通过，覆盖价格版本滚动、并发准入、请求结算和充值幂等。
- 串行全量测试：746 项，745 通过、0 失败、1 跳过；跳过项为需要显式本地开发服务环境的 smoke 测试。
- `pnpm run build`：通过，Next.js 生产构建与静态页面生成成功。
- `pnpm run lint`：0 errors，193 warnings；warning 均位于本次未改动的既有模板或共享代码。
- `git diff --check`：通过。

## 关键守护点

- 分组折扣表单和服务不再读写人工冒烟标记。
- 目录配置不完整时请求 fail closed；配置变化自动滚动路由与价格快照。
- 历史请求锁定旧价格版本，不受后续改价影响。
- 网关钱包接口拒绝任意调额，只允许对既有请求扣费冲正。
- webhook 重放只保留一条本地钱包充值流水，旧 `credit` 与 `apipool_ledger_entry` 均不新增。
- 后台概览资金信号读取本地钱包，不再读取旧 New API 调额账本。

## 未覆盖

- 未连接生产 New API 或真实支付渠道。
- 未执行生产发布后的真实请求 smoke；发布时仍须按 `docs/07-runbook.md` 完成目录同步、支付、网关和钱包 smoke。
