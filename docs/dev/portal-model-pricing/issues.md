# 门户模型定价开发遗留

- [x] 按既定阶段二需求实现与门户钱包解耦的 New API 大额内部运行池：首次供应使用绝对目标值幂等覆盖，存量绑定由后台一次性补齐，之后每小时只监控水位，人工命令显式补充并审计。门户充值/调额仍只写本地钱包；真实上游复测留在 [`../../test/portal-model-pricing/issues.md`](../../test/portal-model-pricing/issues.md)。
- [ ] images edits 请求当前受 25 MiB 上限保护并整体缓冲；内测验证稳定后，另立 feature 改为临时文件或对象存储流式落盘与透传，避免大图编辑请求长期占用进程内存。
